# Builds and caches
node_modules
dist
.vercel
.browsers

# Tests and reports
coverage
test-results
playwright-report
blob-report
**/**/playwright/.cache

# We use pnpm, so prevent other lockfiles from being committed
package-lock.json
yarn.lock

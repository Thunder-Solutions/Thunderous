{
	"name": "thunderous-docs",
	"version": "2.4.6",
	"description": "Documentation for the Thunderous NPM library.",
	"type": "module",
	"scripts": {
		"dev": "thunderous dev",
		"prebuild": "corepack pnpm --filter thunderous build && corepack pnpm --filter thunderous-csr build && corepack pnpm --filter thunderous-server build",
		"build": "thunderous build",
		"clean": "rm -rf dist",
		"start": "pnpm dlx serve dist",
		"format": "prettier . --check --ignore-path ../.gitignore --ignore-path ../.prettierignore",
		"format:fix": "prettier . --write --ignore-path ../.gitignore --ignore-path ../.prettierignore"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^22.7.5",
		"thunderous-server": "workspace:*",
		"typescript": "^5.6.3"
	},
	"dependencies": {
		"highlight.js": "^11.10.0",
		"thunderous": "workspace:*",
		"thunderous-csr": "workspace:*"
	}
}
